version: '3'
services:
  backend:
    build: ./backend/
    tty: true
    volumes:
      - './backend/:/usr/src/app/'
    ports:
      - '8080:8080'
    depends_on:
      - mongo
      - redis
  websocket:
    build: ./websocket/
    tty: true
    volumes:
      - './websocket/:/usr/src/app/'
    ports:
      - '8000:8000'
    depends_on:
      - redis
  mongo:
    image: mongo
    tty: true
  redis:
    image: redis
    tty: true
